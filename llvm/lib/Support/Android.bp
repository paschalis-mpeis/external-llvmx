cc_library_static {
    name: "xlibLLVMSupport",
    vendor_available: true,
    defaults: ["xllvm-defaults-no-generated-headers"],
    target: {
        // llvm-defaults turns off host builds by default, turn it back on for
        // modules that are used to build llvm-tblgen
        host: {
            enabled: true,
        },
        windows: {
            enabled: true,
        },
    },
    srcs: [
        "*.c",
        "*.cpp",
    ],
}
